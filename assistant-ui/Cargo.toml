[package]
name = "assistant-ui"
version = "0.1.0"
edition = "2021"

[dependencies]
tauri = { version = "1", features = ["api-all"] }
tokio = { version = "1", features = ["rt-multi-thread", "macros", "process", "sync"] }
serde_json = "1"
